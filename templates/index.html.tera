{% extends "application.html.tera" %}
{% block content %}
  <h2>Showing {{ posts | length }} Posts</h2>
  {% for post in posts %}
    <div id="post-{{ post.id }}" class="post">
      <h3>{{ post.title }}</h3>
      {{ post.body }}
    </div>
  {% endfor %}
{% endblock content %}
